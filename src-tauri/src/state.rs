use std::sync::Arc;

use crate::audio::audio::{ AudioT};

// pub type State = Arc<tokio::sync::RwLock<State_>>;

// pub struct State_ {
//     pub audio: AudioT,
// }

// pub fn new_state() -> State {
//     Arc::new(tokio::sync::RwLock::new(State_ { audio:  }))
// }
