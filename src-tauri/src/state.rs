use std::sync::Arc;

use crate::audio::audio::Audio;

pub struct State {
    pub audio: Audio,
}

impl State {
    // pub fn new() -> Arc<tokio::sync::RwLock<Self>> {
    //     Arc::new(tokio::sync::RwLock::new(Self {

    //     }))
    // }
}
